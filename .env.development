# Backend local (desarrollo)
# PREVIEW #180 (local, NO commitear): API viaja por el proxy de Vite (vite.config.ts server.proxy)
# hacia https://app.bullwebchile.com — mismo origen, sin CORS.
VITE_API_URL=/api
VITE_WS_URL=ws://localhost:4200
VITE_ENV=development
VITE_CONTACT_WHATSAPP=56937458347
